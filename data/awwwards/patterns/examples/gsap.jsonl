{"slug": "artem-shcherban-portfolio", "snippet": "rray(items);\n  config = config || {};\n  gsap.context(() => { // use a context so that if this is called from within another context or a gsap.matchMedia(), we can perform proper cleanup like the \"resize\" event handler on the window\n  let onChange = config.onChange,\n  lastIndex = 0,\n  tl = gsap.timeline({repeat: config.repeat, onUpdate: onChange && function() {\n  let i = tl.closestIndex();\n  if (lastIndex !== i) {\n  lastIndex = i;\n  onChange(items[i], i);\n  }\n  }, paused: config.paused, defaults: {ease: \"none\"}, onReverseComplete: () => tl.totalTime(tl.rawTime() + tl.duration() * 100)}),\n  leng /* ... */"}
{"slug": "artem-shcherban-portfolio", "snippet": "em\",\n  isMobile: true\n  };\n\nlet currentConfig;\n\n// Build Function\n  function build() {\n  // Kill timeline, revert splitText, etc.\n  tl && tl.kill();\n  menuLinks && menuLinks.revert();\n  menuInfo && menuInfo.revert();\n\n// Create timeline, assign values to dynamic variables\n  tl = gsap.timeline({paused: true}, );\n  menuLinks = SplitText.create(menuContent.querySelector(\".menu_links\").children, {type: \"lines\", mask: \"lines\"});\n  menuInfo = SplitText.create(menuContent.querySelector(\".menu_info\").children,  {type: \"lines\", mask: \"lines\"});\n\n// Build the timeline\n  tl\n  .fromTo(\n  menuToggleMa"}
{"slug": "artem-shcherban-portfolio", "snippet": "}\n  },\n  onStart: () => {\n  if (isShowreel) {\n  gsap.set(\".navbar_logo-wrapper\", {zIndex: 999});\n  gsap.set(\".menu_toggle\", {zIndex: 999});\n  }\n  else {\n  gsap.set(navbar, {zIndex: 999});\n  }\n  gsap.to(logo, { fill: isShowreel ? \"#ffffff\" : \"#0f0f0f\", duration: 0.6, ease: \"power2.inOut\"});\n  },\n  },\n  \"<\"\n  )\n  }\n  else {\n  tl.fromTo(\n  menu,\n  { pointerEvents: \"none\", opacity: 1, width: 0 },\n  { pointerEvents: \"auto\", opacity: 1, width: () => menuContent.offsetWidth, duration: 0.6, ease: \"power2.inOut\" },\n  \"<\"\n  )\n  ."}
{"slug": "ava-srg", "snippet": ".length === 0) { resolve(); return; }\n\n// Ждем выполнения всех обещаний\n\nPromise.all(mediaPromises).then(() => setTimeout(resolve, 100));\n\n});\n\n}\n\n// --- 2. АНИМАЦИЯ ЦИФР ---\n\nfunction animateCounter(element, duration) {\n\nconst counterObject = { value: 0 };\n\nreturn gsap.to(counterObject, {\n\nvalue: 100,\n\nduration: duration,\n\nease: \"expoScale(0.5,7,none)\",\n\nonUpdate: function() {\n\nelement.textContent = Math.round(counterObject.value) + \"%\";\n\n}\n\n});\n\n}\n\n// --- 3. ГЛАВНАЯ ФУНКЦИЯ ---\n\nfunction initPreloader() {\n\n// === НАСТРОЙКИ ===\n\nconst my3dClass = '.hand'; // <-- ВАШ КЛАСС\n\nconst screensToChec /* ... */"}
{"slug": "ava-srg", "snippet": "const preloaderTextItems = document.querySelectorAll('.preloader_text');\n\nif (!preloaderElement) return;\n\nconst maxLineHeight = greyLine ? greyLine.offsetHeight : 300; // Фолбэк высоты\n\nlet contentLoaded = false;\n\nlet minTimePassed = false;\n\nconst preloaderTimeline = gsap.timeline({ delay: 0.5 });\n\n// ЗАПУСКАЕМ ПРОВЕРКУ ЗАГРУЗКИ\n\nwaitForMedia(screensToCheck, my3dClass).then(() => {\n\ncontentLoaded = true;\n\n// Если время уже вышло, а мы ждали только контент — запускаем выход\n\ncheckCompletion();\n\n});\n\nfunction checkCompletion() {\n\n// Если и в"}
{"slug": "ava-srg", "snippet": "lay: 'none' });\n\n}\n\ndocument.addEventListener('DOMContentLoaded', initPreloader);\n\n</script>\n\n<script>\n\ndocument.addEventListener(\"DOMContentLoaded\", () => {\n\n// Если уже согласился — не показываем\n\nif (localStorage.getItem(\"cookies_accepted\") === \"true\") return;\n\nconst cookieTl = gsap.timeline({ paused: true });\n\ncookieTl\n\n.to(\".cookies\", { display: \"block\", visibility: \"visible\" }, 0)\n\n.from(\".cookies\", {\n\nscaleY: 0,\n\ntransformOrigin: \"bottom center\",\n\nduration: 0.8,\n\nease: \"power3.out\"\n\n})\n\n.from(\".cookies_txt-wrap\", {\n\nautoAlpha: 0,\n\ny: 10,"}
{"slug": "c-design-by-dylan", "snippet": "e lijn\n  randomLine.appendChild(glow);\n\n// Startpositie: net boven de top, onzichtbaar\n  gsap.set(glow, {\n  y: \"-16em\",\n  opacity: 0\n  });\n\nconst moveDuration = 2.5;  // langer dan eerst\n  const pauseAfter  = 1;  // pauze na elke swoosh\n\nconst tl = gsap.timeline({\n  onComplete: () => {\n  gsap.delayedCall(pauseAfter, animateGlow);\n  }\n  });\n\n// Hoofdbeweging: snel op gang, langzaam uitrollen\n  tl.to(glow, {\n  y: \"110vh\",\n  duration: moveDuration,\n  ease: \"power3.out\"  // snel begin, zachte slowdown\n  }, 0);\n\n// Snell"}
{"slug": "c-design-by-dylan", "snippet": "gsap.set(\".divs.is-pt\", { opacity: 0.15 });\n\nconst revealOut = () => {\n  showOverlay();\n\nif (prefersReduced) {\n  gsap.set(allCols, { scaleX: 0 });\n  gsap.set(\".divs.is-pt\", { opacity: 0 });\n  hideOverlay();\n  return;\n  }\n\nconst cols = getActiveCols();\n\nconst tl = gsap.timeline({\n  onComplete: () => {\n  hideOverlay();\n  // reset voor volgende page leave\n  gsap.set(allCols, { transformOrigin: \"left\", scaleX: 1 });\n  gsap.set(\".divs.is-pt\", { opacity: 0.15 });\n  }\n  });\n\n// Kolommen weg naar rechts + grid fade iets eerder\n  tl.set(cols, { transformOrigin: \""}
{"slug": "c-design-by-dylan", "snippet": "(onDone) => {\n  showOverlay();\n\nif (prefersReduced) {\n  gsap.set(allCols, { scaleX: 1 });\n  onDone && onDone();\n  return;\n  }\n\nconst cols = getActiveCols();\n\n// Zorg dat grid overlay weer zichtbaar is tijdens cover-in\n  gsap.set(\".divs.is-pt\", { opacity: 0.15 });\n\ngsap.fromTo(\n  cols,\n  { scaleX: 0, transformOrigin: \"left\" },\n  {\n  scaleX: 1,\n  duration: 0.55,\n  ease: \"power4.inOut\",\n  stagger: { each: 0.06, from: \"start\" },\n  onComplete: () => onDone && onDone()\n  }\n  );\n  };\n\n// Run reveal after alles geladen is\n  window.addEventListener(\"load\", revealOut);\n\n// BFCACHE fix (Safari back/forwa /* ... */"}
{"slug": "champions-for-good", "snippet": "x === -25) status = '2-before';\n  else if (cfg.x === 45)  status = '3-after';\n  else if (cfg.x === -45) status = '3-before';\n  else  status = 'hidden';\n\ncard.setAttribute('data-flick-cards-item-status', status);\n  card.style.zIndex = cfg.z;\n\ngsap.to(card, {\n  duration: 0.6,\n  ease: 'elastic.out(1.2, 1)',\n  xPercent: cfg.x,\n  yPercent: cfg.y,\n  rotation: cfg.rot,\n  scale: cfg.s,\n  opacity: cfg.o\n  });\n  });\n  }\n\nrenderCards(activeIndex);\n\nif (total < 7) {\n  console.log('Not minimum of 7 cards');\n  return;\n  }\n\nlet pressClien"}
{"slug": "champions-for-good", "snippet": "const raw = this.x / sliderWidth;\n  let shift = 0;\n  if (raw > threshold) shift = -1;\n  else if (raw < -threshold) shift = 1;\n\nif (shift !== 0) {\n  activeIndex = (activeIndex + shift + total) % total;\n  renderCards(activeIndex);\n  }\n\ngsap.to(this.target, { x: 0, duration: 0.3, ease: 'power1.out' });\n\n// Weiterleitung von Klicks, wenn kaum gezogen wurde\n  if (dragDistance < 4) {\n  this.target.style.pointerEvents = 'none';\n  requestAnimationFrame(() => {\n  requestAnimationFrame(() => {\n  const el = document.elementFromPoint(releaseX, releaseY)"}
{"slug": "darknode", "snippet": "scrub: 1,\n  onUpdate: function(self) {\n  const progress = self.progress; \n  const totalSquares = squareData.length; \n  const visibleCount = Math.floor(totalSquares * progress);\n\nsquareData.forEach((data, index) => {\n  if (index < visibleCount) {\n  gsap.to(data.element, { \n  opacity: 1, \n  duration: 0.5,\n  ease: \"power2.out\"\n  });\n  } else {\n  gsap.to(data.element, { \n  opacity: 0, \n  duration: 0.5,\n  ease: \"power2.out\"\n  });\n  }\n  });\n  }\n  });\n  }\n\n// Инициализация\n  document.addEventListener('DOMContentLoaded', function() {\n  createGr"}
{"slug": "darknode", "snippet": "nt = Math.floor(totalSquares * progress);\n\nsquareData.forEach((data, index) => {\n  if (index < visibleCount) {\n  gsap.to(data.element, { \n  opacity: 1, \n  duration: 0.5,\n  ease: \"power2.out\"\n  });\n  } else {\n  gsap.to(data.element, { \n  opacity: 0, \n  duration: 0.5,\n  ease: \"power2.out\"\n  });\n  }\n  });\n  }\n  });\n  }\n\n// Инициализация\n  document.addEventListener('DOMContentLoaded', function() {\n  createGrid('.hero_section');\n  setupAnimation('.hero_section');\n\ncreateGrid('.footer');\n  //setupAnimation('.footer');\n  });\n</script>\n\n<script>\n  document.add"}
{"slug": "darknode", "snippet": "}\n\n// 2. ПРИСВОЮЄМО ЗНАЧЕННЯ ЗОВНІШНІЙ ЗМІННІЙ\n            splitInstance = new SplitText(link, { type: \"chars\" });\n            const chars = splitInstance.chars; // Використовуємо її\n            const originalChars = chars.map(char => char.textContent);\n\ncurrentAnimation = gsap.timeline();\n\nchars.forEach((char, index) => {\n                let proxy = { frame: 0 }; \n                currentAnimation.to(proxy, {\n                    frame: 10,\n                    duration: 0.15,\n                    ease: \"none\",\n                    onUpdate: () => {\n                        char.textContent = getRa /* ... */"}
{"slug": "emilie-aubry", "snippet": "tion() {\n\nif (typeof gsap === 'undefined') {\n\npreloader.style.display = 'none';\n\ndocument.body.classList.remove('is-loading');\n\ndocument.body.classList.add('no-preloader');\n\nreturn;\n\n}\n\nconst tl = gsap.timeline({\n\nonComplete: () => {\n\npreloader.style.display = 'none';\n\ndocument.body.classList.remove('is-loading');\n\ndocument.body.classList.add('no-preloader');\n\n// Reset inline styles on logo just in case, though it's hid"}
{"slug": "farm-minerals", "snippet": ";\n  ScrollTrigger.create({\n  trigger: triggerElement,\n  start: \"top 90%\",\n  onEnter: () => timeline.play()\n  });\n  }\n\ndocument.querySelectorAll(\"[text-split]\").forEach(el => {\n  const split = new SplitText(el, { type: \"chars, words, lines\" });\n\nconst tl = gsap.timeline({ paused: true });\n\ngsap.set(split.chars, { autoAlpha: 0 });\n\ntl.fromTo(\n  split.chars,\n  { autoAlpha: 0 },\n  {\n  autoAlpha: 1,\n  duration: 0.4,\n  ease: \"power2.out\",\n  stagger: {\n  each: 0.02,\n  from: \"random\",\n  },\n  }"}
{"slug": "farm-minerals", "snippet": "}\n  );\n\ncreateScrollTrigger(el, tl);\n  });\n\ndocument.querySelectorAll(\"[text-split-delay]\").forEach(el => {\n  const delayValue = parseFloat(el.getAttribute(\"text-split-delay\")) || 0;\n  const split = new SplitText(el, { type: \"chars, words, lines\" });\n\nconst tl = gsap.timeline({ paused: true });\n\ngsap.set(split.chars, { autoAlpha: 0 });\n\ntl.fromTo(\n  split.chars,\n  { autoAlpha: 0 },\n  {\n  autoAlpha: 1,\n  duration: 0.3,\n  ease: \"power2.out\",\n  stagger: {\n  each: 0.02,\n  from: \"random\",\n  },"}
{"slug": "farm-minerals", "snippet": "mage(img, x, y, iw * scale, ih * scale);\n}\n\n// --- Перерисовка при ресайзе ---\nwindow.addEventListener(\"resize\", () => {\n  setCanvasSize();\n  drawFrame(Math.round(frameObj?.frame || 0));\n});\n\nlet frameObj = { frame: 0 };\n\n// --- ScrollTrigger-анимация ---\nfunction startScroll() {\n  drawFrame(0);\n\ngsap.to(frameObj, {\n  frame: total - 1,\n  ease: \"none\",\n  scrollTrigger: {\n  trigger: \".capsule\",\n  start: \"top top\",\n  end: \"bottom bottom\",\n  scrub: 1\n  },\n  onUpdate: () => {\n  const i = Math.round(frameObj.frame);\n  drawFrame(i);\n  }\n  });\n}\n</script>\n\n<script>\n(() => {\n  // Цвета\n  const LIGHT =  /* ... */"}
{"slug": "jason-bergh", "snippet": "duplicateChars.forEach((dup, i) => {\n  if (dup) {\n  dup.style.left = charPositions[i].left + 'px';\n  dup.style.top = charPositions[i].top + 'px';\n  }\n  });\n\nisAnimating = true;\n  currentTl = gsap.timeline({\n  onComplete: () => { isAnimating = false; }\n  });\n\noriginalChars.forEach((char, i) => {\n  const fromBottom = i % 2 === 0;\n  currentTl.to(char, {\n  yPercent: fromBottom ? -100 : 100,\n  duration: 0.5,\n  ease: 'pow"}
{"slug": "jason-bergh", "snippet": "ease: 'power3.inOut'\n  }, 0);\n  });\n  };\n\nconst onLeave = (e) => {\n  if (e.relatedTarget && element.contains(e.relatedTarget)) return;\n\nif (currentTl) currentTl.kill();\n\nisAnimating = true;\n  currentTl = gsap.timeline({\n  onComplete: () => { isAnimating = false; }\n  });\n\noriginalChars.forEach((char, i) => {\n  const fromBottom = i % 2 === 0;\n  currentTl.to(char, {\n  yPercent: 0,\n  duration: 0.5,\n  ease: 'power3.inOut'"}
{"slug": "jason-bergh", "snippet": "eof SplitType !== 'undefined'; },\n\n_formatTime: (s) => !s || isNaN(s)\n  ? '00:00'\n  : `${Math.floor(s / 60).toString().padStart(2, '0')}:${Math.floor(s % 60).toString().padStart(2, '0')}`,\n\n_animateElement(el, pos) {\n  if (!el) return;\n  this._hasGSAP()\n  ? gsap.to(el, { yPercent: pos, duration: 0.3, ease: \"power2.out\" })\n  : (el.style.transform = `translateY(${pos}%)`);\n  },\n\n_setElementPosition(el, yPercent) {\n  if (!el) return;\n  this._hasGSAP()\n  ? gsap.set(el, { yPercent })\n  : (el.style.transform = `translateY(${yPercent}%)`);\n  },\n\n_setRadialGradientOpacit"}
{"slug": "momoney", "snippet": "ctx.globalAlpha = 1;\n  if (gridBorderSize > 0) {\n  ctx.lineWidth  = gridBorderSize;\n  ctx.strokeStyle = gridBorderColor;\n  ctx.strokeRect(b.x, b.y, squareSize, squareSize);\n  }\n  });\n  requestAnimationFrame(draw);\n  }\n\nfunction fadeOut(b) {\n  gsap.to(b, { alpha: 0, duration: 2, delay: 0.5 });\n  }\n\nfunction supportsTouch() {\n  return 'ontouchstart' in window || navigator.maxTouchPoints > 1;\n  }\n\nif (!supportsTouch()) {\n  document.addEventListener('mousemove', function onPointerMove(e) {\n  const r = canvas.getBoundingClientRect();\n  const mx = e.clientX, m"}
{"slug": "momoney", "snippet": "reSize);\n  const yIdx = Math.floor((my - r.top)  / squareSize);\n  const idx  = yIdx * cols + xIdx;\n  if (idx !== lastHovered) {\n  const b = blocks[idx];\n  b.color = gridColors[colorPointer];\n  colorPointer = (colorPointer + 1) % gridColors.length;\n  gsap.to(b, { alpha: 1, duration: 0.1, overwrite: true });\n  fadeOut(b);\n  trailQueue.push(b);\n  if (trailQueue.length > gridColors.length) {\n  const old = trailQueue.shift();\n  gsap.killTweensOf(old);\n  old.alpha = 0;\n  }\n  lastHovered = idx;\n  }\n  });\n  }\n\nwindow.addEv"}
{"slug": "nicola-romeitm", "snippet": "ding\"]');e.forEach(e=>{gsap.set(e,{autoAlpha:1});let l=new SplitText(e,{type:\"lines\",linesClass:\"mask-line\"});l.lines.forEach(e=>{let l=document.createElement(\"div\");for(l.style.display=\"block\";e.firstChild;)l.appendChild(e.firstChild);e.appendChild(l)});let t=e.querySelectorAll(\".mask-line > div\");gsap.from(t,{yPercent:100,duration:.85,stagger:.05,ease:\"power4.out\",scrollTrigger:{trigger:e,start:\"top 95%\",toggleActions:\"play none none none\"}})})});\n</script><script src=\"https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js\"></script>\n<script src=\"https://cdn.jsdelivr.net/npm/virtu /* ... */"}
